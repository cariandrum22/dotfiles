;;; init-which-key.el --- Which-key configuration -*- lexical-binding: t -*-
;;; Commentary:
;; Configuration for which-key keybinding hints.
;;; Code:

(use-package which-key
  :config
  (which-key-mode))

(provide 'init-which-key)
;;; init-which-key.el ends here
