;;; init-shell.el --- Shell mode configuration -*- lexical-binding: t -*-
;;; Commentary:
;; Configuration for shell script editing.
;;; Code:

(setq sh-basic-offset 2
      sh-indentation 2
      sh-shell-file "/usr/bin/env bash")

(provide 'init-shell)
;;; init-shell.el ends here
